package Application;

import javafx.scene.image.ImageView;

public class Multiplier extends Token{
    public Multiplier(int value, String name, ImageView image) {
        super(value, name, image);
    }
}
