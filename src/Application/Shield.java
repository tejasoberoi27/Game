package Application;

import javafx.scene.image.ImageView;

public class Shield extends Token {
    public Shield(int value, String name, ImageView image) {
        super(value, name, image);
    }
}
